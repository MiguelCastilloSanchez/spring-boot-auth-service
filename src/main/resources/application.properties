# Global aplication configuration
spring.application.name=auth-service
spring.application.port=8080

# MongoDB configuration
spring.data.mongodb.uri=mongodb://${MONGO_USERNAME}:${MONGO_PASSWORD}@${MONGO_HOST}:${MONGO_PORT}/users_db?authSource=admin

# JWT configuration
api.security.token.secret=${JWT_SECRET}

# Redis configuration
spring.data.redis.host=${REDIS_HOST}
spring.data.redis.port=${REDIS_PORT}

# RabbitMQ configuration
spring.rabbitmq.host=${RABBITMQ_HOST}
spring.rabbitmq.port=${RABBITMQ_PORT}
spring.rabbitmq.username=${RABBITMQ_USERNAME}
spring.rabbitmq.password=${RABBITMQ_PASSWORD}

spring.rabbitmq.exchange.app=app-exchange

spring.rabbitmq.queue.user=user-service-queue
spring.rabbitmq.queue.auth=auth-service-queue

spring.rabbitmq.routing.key.user=user.register


